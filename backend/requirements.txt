# PRODUCTION REQUIREMENTS FOR RENDER DEPLOYMENT
# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Production ASGI server (better than uvicorn for production)
gunicorn==21.2.0

# Database clients with compatible versions
supabase==1.3.0
pymongo==4.6.0

# HTTP and async dependencies (pin these for stability)
httpx==0.24.1
requests==2.31.0

# Environment and utility
python-dotenv==1.0.0
beautifulsoup4==4.12.2

# Web scraping dependencies
lxml==4.9.3
html5lib==1.1

# Translation dependencies (for Google Translate API)
deep-translator==1.11.4

# Additional production dependencies
typing-extensions==4.8.0

# Optional but recommended for production logging
coloredlogs==15.0.1

# Remove asyncio==3.4.3 (this is built into Python 3.7+)
# Remove gotrue and httpcore comments (not needed)